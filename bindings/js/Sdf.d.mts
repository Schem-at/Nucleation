// generated by diplomat-tool
import type { NucleationError } from "./NucleationError.mjs"
import type { Schematic } from "./Schematic.mjs"
import type { SdfAxis } from "./SdfAxis.mjs"
import type { SdfBounds } from "./SdfBounds.mjs"
import type { SdfCellMode } from "./SdfCellMode.mjs"
import type { SdfNormal } from "./SdfNormal.mjs"
import type { Shape } from "./Shape.mjs"
import type { pointer, codepoint } from "./diplomat-runtime.d.ts";



/**
 * An immutable, composable signed-distance-field expression graph.
 *
 * Primitive constructors and every combinator return a new graph, so values
 * can be shared safely between Flow nodes and across Kotlin/Java, JavaScript,
 * and Python bindings. JSON is retained only for explicit import/export and
 * the legacy sampling helpers.
 */
export class Sdf {
    /** @internal */
    get ffiValue(): pointer;
    /** @internal */
    constructor();


    static sphere(radius: number): Sdf;

    /**
     * Axis-aligned rounded box, centered at the origin.
     */
    static boxShape(halfX: number, halfY: number, halfZ: number, rounding: number): Sdf;

    static ellipsoid(radiusX: number, radiusY: number, radiusZ: number): Sdf;

    static torus(majorRadius: number, minorRadius: number): Sdf;

    static capsule(ax: number, ay: number, az: number, bx: number, by: number, bz: number, radius: number): Sdf;

    static cappedCylinder(radius: number, halfHeight: number): Sdf;

    static cappedCone(halfHeight: number, bottomRadius: number, topRadius: number): Sdf;

    static plane(normalX: number, normalY: number, normalZ: number, offset: number): Sdf;

    static octahedron(size: number): Sdf;

    static hexPrism(radius: number, halfHeight: number): Sdf;

    static superPrism(halfX: number, halfY: number, halfZ: number, exponent: number): Sdf;

    static cells(frequency: number, seed: number, jitter: number, mode: SdfCellMode, threshold: number): Sdf;

    unionWith(other: Sdf): Sdf;

    intersectionWith(other: Sdf): Sdf;

    subtract(other: Sdf): Sdf;

    smoothUnion(other: Sdf, radius: number): Sdf;

    smoothSubtract(other: Sdf, radius: number): Sdf;

    smoothIntersection(other: Sdf, radius: number): Sdf;

    rounded(radius: number): Sdf;

    shell(thickness: number): Sdf;

    translate(x: number, y: number, z: number): Sdf;

    rotate(xDegrees: number, yDegrees: number, zDegrees: number): Sdf;

    scale(factor: number): Sdf;

    mirror(axis: SdfAxis): Sdf;

    repeatInfinite(spacingX: number, spacingY: number, spacingZ: number): Sdf;

    repeatCounted(spacingX: number, spacingY: number, spacingZ: number, countX: number, countY: number, countZ: number): Sdf;

    displace(amplitude: number, frequency: number, seed: number, octaves: number): Sdf;

    warp(amplitude: number, frequency: number, seed: number): Sdf;

    evalAt(x: number, y: number, z: number): number;

    normal(x: number, y: number, z: number, epsilon: number): SdfNormal;

    bounds(): SdfBounds;

    toShape(): Shape;

    toShapeBounded(minX: number, minY: number, minZ: number, maxX: number, maxY: number, maxZ: number): Shape;

    static fromJsonString(json: string): Sdf;

    toJson(): string;

    /**
     * Legacy JSON-first terrain helper. Prefer typed constructors and
     * `to_shape()` with `BuildingTool.fill()` for new code.
     */
    static schematicFromSdfAuto(sdfJson: string, rulesJson: string): Schematic;

    /**
     * Legacy JSON-first terrain helper with optional explicit bounds.
     */
    static schematicFromSdf(sdfJson: string, rulesJson: string, hasBounds: boolean, minX: number, minY: number, minZ: number, maxX: number, maxY: number, maxZ: number): Schematic;

    /**
     * Legacy JSON-first evaluator. Prefer `Sdf.from_json_string(...).eval_at(...)`.
     */
    static eval(sdfJson: string, x: number, y: number, z: number): number;
}