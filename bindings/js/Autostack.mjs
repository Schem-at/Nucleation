// generated by diplomat-tool
import { NucleationError } from "./NucleationError.mjs"
import { Schematic } from "./Schematic.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const Autostack_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.Autostack_destroy(ptr);
});

/**
 * Free functions in the old ABI hung directly off `schematic_*`; here they get a
 * namespacing opaque-less home via static methods on a zero-size type is not
 * supported, so they live as static methods taking `&Schematic` explicitly.
 */
export class Autostack {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("Autostack is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            Autostack_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Detect repeating structures (region coverage). Writes a JSON array string;
     * each element has `mode`, `vectors`, `coverage`, `region_min`/`region_max`,
     * `cell_min`/`cell_max`, `label`.
     */
    static detectStructures(schematic) {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.Autostack_detect_structures(schematic.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Graph-based detection: recovers diagonal lattice periods via the redstone
     * logic graph. Writes `"[]"` for non-redstone builds. Requires the
     * `simulation` feature; writes `"[]"` when compiled without it.
     */
    static detectStructuresGraph(schematic) {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.Autostack_detect_structures_graph(schematic.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Resize a 1D / diagonal structure along its period vector.
     */
    static resize1d(schematic, vx, vy, vz, units) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.Autostack_resize_1d(diplomatReceive.buffer, schematic.ffiValue, vx, vy, vz, units);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new Schematic(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Resize a 2D structure to `n1`×`n2` cells along the two period vectors.
     */
    static resize2d(schematic, v1x, v1y, v1z, v2x, v2y, v2z, n1, n2) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.Autostack_resize_2d(diplomatReceive.buffer, schematic.ffiValue, v1x, v1y, v1z, v2x, v2y, v2z, n1, n2);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new Schematic(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}