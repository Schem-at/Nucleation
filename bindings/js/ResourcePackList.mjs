// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const ResourcePackList_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.ResourcePackList_destroy(ptr);
});

/**
 * Ordered list of raw resource-pack ZIP buffers, lowest priority first.
 * Feed it to {@link ResourcePack::from_list}.
 */
export class ResourcePackList {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("ResourcePackList is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            ResourcePackList_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    static create() {

        const result = wasm.ResourcePackList_create();

        try {
            return new ResourcePackList(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Append one resource-pack ZIP buffer. Later buffers overlay earlier
     * ones on per-key collision (Minecraft pack-ordering semantics).
     */
    add(data) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const dataSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.slice(wasm, data, "u8")));
    wasm.ResourcePackList_add(this.ffiValue, dataSlice.ptr);

        try {}

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

        }
    }

    len() {

        const result = wasm.ResourcePackList_len(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}