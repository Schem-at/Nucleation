// generated by diplomat-tool
import type { NucleationError } from "./NucleationError.mjs"
import type { pointer, codepoint } from "./diplomat-runtime.d.ts";



/**
 * Immutable hashed-cell variation shared by coordinated SDF source layers.
 */
export class CellularSdfConfig {
    /** @internal */
    get ffiValue(): pointer;
    /** @internal */
    constructor();


    /**
     * Validates every field up front, so a config that constructs here is
     * never rejected for its own values by a later `cellular_sdf` call.
     */
    static create(cellSizeX: number, cellSizeZ: number, seed: bigint, maxJitterX: number, maxJitterZ: number, maxYawDegrees: number, minScale: number, maxScale: number, minYOffset: number, maxYOffset: number, presenceNumerator: number, presenceDenominator: number, featureSalt: bigint): CellularSdfConfig;
}