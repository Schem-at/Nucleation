// generated by diplomat-tool
import type { NucleationError } from "./NucleationError"
import type { ResourcePackList } from "./ResourcePackList"
import type { TextureInfo } from "./TextureInfo"
import type { pointer, codepoint } from "./diplomat-runtime.d.ts";



/**
 * A loaded (possibly merged) Minecraft resource pack.
 * Wraps {@link crate::meshing::ResourcePackSource}.
 */
export class ResourcePack {
    /** @internal */
    get ffiValue(): pointer;
    /** @internal */
    constructor();


    /**
     * Load a resource pack from an in-memory ZIP buffer.
     */
    static fromBytes(data: Array<number>): ResourcePack;

    /**
     * Load and merge multiple resource packs, lowest priority first.
     */
    static fromList(list: ResourcePackList): ResourcePack;

    blockstateCount(): number;

    modelCount(): number;

    textureCount(): number;

    /**
     * Namespaces present in the pack, as a JSON array string.
     */
    namespacesJson(): string;

    /**
     * All blockstate identifiers, as a JSON array string.
     */
    listBlockstatesJson(): string;

    /**
     * All model identifiers, as a JSON array string.
     */
    listModelsJson(): string;

    /**
     * All texture identifiers, as a JSON array string.
     */
    listTexturesJson(): string;

    getBlockstateJson(name: string): string;

    getModelJson(name: string): string;

    /**
     * Texture metadata (size, animation flag, frame count).
     */
    getTextureInfo(name: string): TextureInfo;

    /**
     * Raw RGBA pixels of a texture, base64-encoded.
     */
    getTexturePixelsB64(name: string): string;

    addBlockstateJson(name: string, json: string): void;

    addModelJson(name: string, json: string): void;

    /**
     * Add a raw RGBA texture (`pixels` length must be `width * height * 4`).
     */
    addTexture(name: string, width: number, height: number, pixels: Array<number>): void;

    /**
     * Register a MeshExporter with the FormatManager so `save_as("mesh", ...)`
     * works. (Old ABI: `schematic_register_mesh_exporter`.)
     */
    registerMeshExporter(): void;
}