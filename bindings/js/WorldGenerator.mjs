// generated by diplomat-tool
import { Brush } from "./Brush.mjs"
import { CellularSdfConfig } from "./CellularSdfConfig.mjs"
import { GeneratedChunk } from "./GeneratedChunk.mjs"
import { GeneratedChunkOverlayMode } from "./GeneratedChunkOverlayMode.mjs"
import { GeneratedWorldStream } from "./GeneratedWorldStream.mjs"
import { NucleationError } from "./NucleationError.mjs"
import { Sdf } from "./Sdf.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const WorldGenerator_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.WorldGenerator_destroy(ptr);
});

/**
 * An immutable native chunk source graph.
 *
 * Generated bindings expose concrete source constructors rather than host
 * callbacks, so SDF evaluation and block placement stay entirely in Rust.
 */
export class WorldGenerator {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("WorldGenerator is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            WorldGenerator_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Create an SDF-backed source evaluated at voxel centers over the inclusive
     * Y range. `source_id` and `version` become chunk provenance/cache metadata.
     */
    static sdf(volume, material, minY, maxY, sourceId, version) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const sourceIdSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, sourceId)));
        const versionSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, version)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_sdf(diplomatReceive.buffer, volume.ffiValue, material.ffiValue, minY, maxY, sourceIdSlice.ptr, versionSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new WorldGenerator(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Create a sparse infinite source by placing a bounded SDF motif once per
     * deterministically transformed cell. Reuse `config` across layers to keep
     * terrain, water, vegetation, paths, and structures coordinated.
     */
    static cellularSdf(volume, material, minY, maxY, config, sourceId, version) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const sourceIdSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, sourceId)));
        const versionSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, version)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_cellular_sdf(diplomatReceive.buffer, volume.ffiValue, material.ffiValue, minY, maxY, config.ffiValue, sourceIdSlice.ptr, versionSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new WorldGenerator(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Create a sparse source from projected building footprints, including
     * caller-projected OSM-derived data.
     * `buildings_json` uses the same schema as `Geo.extrude_footprints`:
     * `[{"polygon":[[x,z],...],"height":40,"min_y":1,
     * "block":"minecraft:bricks"}]`. `height` is the absolute top Y, matching
     * `Geo.extrude_footprints`. Fetching and lat/lon projection stay
     * caller-controlled; this source rasterizes only requested chunks.
     */
    static projectedFootprints(buildingsJson, baseBlock, sourceId, version) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const buildingsJsonSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, buildingsJson)));
        const baseBlockSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, baseBlock)));
        const sourceIdSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, sourceId)));
        const versionSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, version)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_projected_footprints(diplomatReceive.buffer, buildingsJsonSlice.ptr, baseBlockSlice.ptr, sourceIdSlice.ptr, versionSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new WorldGenerator(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Create an initially empty ordered source composition.
     */
    static composite(sourceId, version) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const sourceIdSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, sourceId)));
        const versionSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, version)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_composite(diplomatReceive.buffer, sourceIdSlice.ptr, versionSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new WorldGenerator(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Append a source to a composite. Later `Replace` layers win at occupied
     * voxels; `KeepExisting` layers only fill air. Errors on non-composites.
     *
     * Streams already created from this generator keep the layer list they
     * were built with; only later `generate`/`stream` calls see the addition.
     */
    addLayer(source, mode) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_add_layer(diplomatReceive.buffer, this.ffiValue, source.ffiValue, mode.ffiValue);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Generate one random-access chunk.
     */
    generate(cx, cz) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_generate(diplomatReceive.buffer, this.ffiValue, cx, cz);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new GeneratedChunk(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Traverse an inclusive chunk rectangle lazily in canonical region-major
     * order. The stream snapshots the generator's sources at creation, so
     * later `add_layer` calls do not affect a stream already in flight.
     */
    stream(minCx, minCz, maxCx, maxCz) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldGenerator_stream(diplomatReceive.buffer, this.ffiValue, minCx, minCz, maxCx, maxCz);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new GeneratedWorldStream(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}