// generated by diplomat-tool
import { BlockPos } from "./BlockPos.mjs"
import { MeshConfig } from "./MeshConfig.mjs"
import { MeshResult } from "./MeshResult.mjs"
import { NucleationError } from "./NucleationError.mjs"
import { ResourcePack } from "./ResourcePack.mjs"
import { Schematic } from "./Schematic.mjs"
import { TextureAtlas } from "./TextureAtlas.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const ChunkMeshResult_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.ChunkMeshResult_destroy(ptr);
});

/**
 * Per-chunk mesh results. Wraps {@link crate::meshing::ChunkMeshResult}.
 */
export class ChunkMeshResult {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("ChunkMeshResult is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            ChunkMeshResult_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Mesh with the default chunk size (old ABI: `schematic_mesh_by_chunk`).
     */
    static create(schematic, pack, config) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.ChunkMeshResult_create(diplomatReceive.buffer, schematic.ffiValue, pack.ffiValue, config.ffiValue);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new ChunkMeshResult(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Mesh with an explicit chunk size (old ABI: `schematic_mesh_by_chunk_size`).
     */
    static createWithSize(schematic, pack, config, chunkSize) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.ChunkMeshResult_create_with_size(diplomatReceive.buffer, schematic.ffiValue, pack.ffiValue, config.ffiValue, chunkSize);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new ChunkMeshResult(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Mesh chunks against a pre-built shared atlas, synchronously
     * (old ABI: `schematic_mesh_chunks_with_atlas`). For progress
     * reporting use {@link MeshJob::start} instead.
     */
    static createWithAtlas(schematic, pack, config, chunkSize, atlas) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.ChunkMeshResult_create_with_atlas(diplomatReceive.buffer, schematic.ffiValue, pack.ffiValue, config.ffiValue, chunkSize, atlas.ffiValue);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new ChunkMeshResult(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Number of chunk meshes.
     */
    chunkCount() {

        const result = wasm.ChunkMeshResult_chunk_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Coordinate of the `index`-th chunk (old ABI:
     * `chunkmeshresult_chunk_coordinates` returned all of them flat).
     */
    chunkCoordinateAt(index) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 13, 4, true);


        const result = wasm.ChunkMeshResult_chunk_coordinate_at(diplomatReceive.buffer, this.ffiValue, index);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return BlockPos._fromFFI(diplomatRuntime.internalConstructor, diplomatReceive.buffer);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * The mesh for one chunk coordinate (cloned).
     */
    getMesh(cx, cy, cz) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.ChunkMeshResult_get_mesh(diplomatReceive.buffer, this.ffiValue, cx, cy, cz);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new MeshResult(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Total vertex count across all chunk meshes.
     */
    totalVertexCount() {

        const result = wasm.ChunkMeshResult_total_vertex_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Total triangle count across all chunk meshes.
     */
    totalTriangleCount() {

        const result = wasm.ChunkMeshResult_total_triangle_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * All chunk meshes serialized in the NUCM cache format, base64-encoded.
     */
    nucmDataB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.ChunkMeshResult_nucm_data_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * NUCM v2 with a shared atlas, base64-encoded.
     */
    nucmDataWithAtlasB64(atlas) {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.ChunkMeshResult_nucm_data_with_atlas_b64(this.ffiValue, atlas.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}