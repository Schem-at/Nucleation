// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const IoLayout_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.IoLayout_destroy(ptr);
});

/**
 * An immutable IO layout. Wraps
 * {@link crate::simulation::typed_executor::IoLayout}.
 */
export class IoLayout {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("IoLayout is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            IoLayout_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Input names as a JSON array string.
     */
    inputNamesJson() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.IoLayout_input_names_json(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Output names as a JSON array string.
     */
    outputNamesJson() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.IoLayout_output_names_json(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}