// generated by diplomat-tool
import { NucleationError } from "./NucleationError.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const Nbt_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.Nbt_destroy(ptr);
});

/**
 * Namespace type for the free-standing NBT builder helpers (the old
 * `nbt_text_build` / `nbt_chest_build` / `nbt_sign_build`), following the
 * static-methods-on-a-dummy-opaque pattern.
 */
export class Nbt {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("Nbt is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            Nbt_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Build a Minecraft JSON text-component string.
     *
     * `color` may be empty (no color). `bold` and `italic` use `-1` for
     * unset, `0` for false, nonzero for true.
     */
    static textBuild(s, color, bold, italic) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const sSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, s)));
        const colorSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, color)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);

        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);


        const result = wasm.Nbt_text_build(diplomatReceive.buffer, sSlice.ptr, colorSlice.ptr, bold, italic, write.buffer);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
            write.free();
        }
    }

    /**
     * Build a chest-NBT SNBT string for use as the `{...}` portion of a block
     * string.
     *
     * `items_json` is a JSON array of `{"id": string, "count"?: int,
     * "slot"?: int}` entries (may be empty or `[]`); a missing/non-positive
     * `count` defaults to 1, a missing/negative `slot` auto-assigns
     * positionally. `name` is an optional plain-text custom name (empty = no
     * `CustomName`); it is wrapped in a JSON text component automatically
     * unless it already starts with `{`.
     */
    static chestBuild(itemsJson, name) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const itemsJsonSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, itemsJson)));
        const nameSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, name)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);

        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);


        const result = wasm.Nbt_chest_build(diplomatReceive.buffer, itemsJsonSlice.ptr, nameSlice.ptr, write.buffer);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
            write.free();
        }
    }

    /**
     * Build a modern (1.20+) sign-NBT SNBT string.
     *
     * `front_json` and `back_json` are JSON arrays of up to 4 line strings
     * (either may be empty or `[]`). Each line may be a plain string
     * (auto-wrapped via `text_build`) or an already-built JSON component
     * (starts with `{`). `color` is the dye color string (empty defaults to
     * `"black"`).
     */
    static signBuild(frontJson, backJson, color, glowing, waxed) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const frontJsonSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, frontJson)));
        const backJsonSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, backJson)));
        const colorSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, color)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);

        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);


        const result = wasm.Nbt_sign_build(diplomatReceive.buffer, frontJsonSlice.ptr, backJsonSlice.ptr, colorSlice.ptr, glowing, waxed, write.buffer);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
            write.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}