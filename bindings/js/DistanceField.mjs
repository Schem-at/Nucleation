// generated by diplomat-tool
import { Schematic } from "./Schematic.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const DistanceField_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.DistanceField_destroy(ptr);
});

export class DistanceField {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("DistanceField is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            DistanceField_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Distance transform of a build's occupied voxels: every solid block
     * learns how many blocks it sits below the surface, and the gradient of
     * that depth gives the outward normal. Computed once over the
     * schematic's bounding box.
     */
    static fromSchematic(schematic) {

        const result = wasm.DistanceField_from_schematic(schematic.ffiValue);

        try {
            return new DistanceField(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Blocks below the surface at a voxel: 0 for empty/outside, 1 at the
     * surface, increasing inward.
     */
    depth(x, y, z) {

        const result = wasm.DistanceField_depth(this.ffiValue, x, y, z);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * The upward component of the outward surface normal: 1 on flat ground,
     * 0 on a vertical face, negative under an overhang. The scalar to key
     * slope-based landscaping on (grass on the flats, stone on the steeps).
     */
    slope(x, y, z) {

        const result = wasm.DistanceField_slope(this.ffiValue, x, y, z);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * The full outward surface normal as JSON `[nx, ny, nz]`.
     */
    normalJson(x, y, z) {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.DistanceField_normal_json(this.ffiValue, x, y, z, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}