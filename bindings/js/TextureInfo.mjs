// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * Texture size/animation metadata for one texture in a resource pack.
 */
export class TextureInfo {
    #width;
    get width() {
        return this.#width;
    }
    set width(value){
        this.#width = value;
    }
    #height;
    get height() {
        return this.#height;
    }
    set height(value){
        this.#height = value;
    }
    #animated;
    get animated() {
        return this.#animated;
    }
    set animated(value){
        this.#animated = value;
    }
    #frameCount;
    get frameCount() {
        return this.#frameCount;
    }
    set frameCount(value){
        this.#frameCount = value;
    }
    /** @internal */
    static fromFields(structObj) {
        return new TextureInfo(structObj);
    }

    #internalConstructor(structObj) {
        if (typeof structObj !== "object") {
            throw new Error("TextureInfo's constructor takes an object of TextureInfo's fields.");
        }

        if ("width" in structObj) {
            this.#width = structObj.width;
        } else {
            throw new Error("Missing required field width.");
        }

        if ("height" in structObj) {
            this.#height = structObj.height;
        } else {
            throw new Error("Missing required field height.");
        }

        if ("animated" in structObj) {
            this.#animated = structObj.animated;
        } else {
            throw new Error("Missing required field animated.");
        }

        if ("frameCount" in structObj) {
            this.#frameCount = structObj.frameCount;
        } else {
            throw new Error("Missing required field frameCount.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 16;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 4;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof TextureInfo) {
            return obj;
        }

        return TextureInfo.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#width, Uint32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#height, Uint32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 8, this.#animated, Uint8Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 12, this.#frameCount, Uint32Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("TextureInfo._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const widthDeref = (new Uint32Array(wasm.memory.buffer, ptr, 1))[0];
        structObj.width = widthDeref;
        const heightDeref = (new Uint32Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.height = heightDeref;
        const animatedDeref = (new Uint8Array(wasm.memory.buffer, ptr + 8, 1))[0] === 1;
        structObj.animated = animatedDeref;
        const frameCountDeref = (new Uint32Array(wasm.memory.buffer, ptr + 12, 1))[0];
        structObj.frameCount = frameCountDeref;

        return new TextureInfo(structObj);
    }


    constructor(structObj) {
        return this.#internalConstructor(...arguments)
    }
}