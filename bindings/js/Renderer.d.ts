// generated by diplomat-tool
import type { NucleationError } from "./NucleationError"
import type { RenderConfig } from "./RenderConfig"
import type { Schematic } from "./Schematic"
import type { pointer, codepoint } from "./diplomat-runtime.d.ts";



/**
 * Namespace type for the render entry points (PORTING rule 12).
 */
export class Renderer {
    /** @internal */
    get ffiValue(): pointer;
    /** @internal */
    constructor();


    /**
     * Render a schematic to raw RGBA pixel bytes, written as base64
     * (PORTING rule 6). `pack_zip` is a resource-pack zip in memory.
     */
    static renderPixelsB64(schematic: Schematic, packZip: Array<number>, config: RenderConfig): string;

    /**
     * Render a schematic to PNG bytes, written as base64 (PORTING rule 6).
     * `pack_zip` is a resource-pack zip in memory.
     */
    static renderPngB64(schematic: Schematic, packZip: Array<number>, config: RenderConfig): string;

    /**
     * Render a schematic to a PNG file at `path`.
     */
    static renderToFile(schematic: Schematic, packZip: Array<number>, config: RenderConfig, path: string): void;
}