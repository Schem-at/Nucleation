// generated by diplomat-tool
import { MeshConfig } from "./MeshConfig.mjs"
import { NucleationError } from "./NucleationError.mjs"
import { ResourcePack } from "./ResourcePack.mjs"
import { Schematic } from "./Schematic.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const RawMeshExport_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.RawMeshExport_destroy(ptr);
});

/**
 * Raw vertex streams for custom rendering. Wraps {@link crate::meshing::RawMeshExport}.
 */
export class RawMeshExport {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("RawMeshExport is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            RawMeshExport_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Export raw mesh data (old ABI: `schematic_to_raw_mesh`).
     */
    static create(schematic, pack, config) {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.RawMeshExport_create(diplomatReceive.buffer, schematic.ffiValue, pack.ffiValue, config.ffiValue);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new RawMeshExport(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
        }
    }

    /**
     * Number of vertices in the exported mesh.
     */
    vertexCount() {

        const result = wasm.RawMeshExport_vertex_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Number of triangles in the exported mesh.
     */
    triangleCount() {

        const result = wasm.RawMeshExport_triangle_count(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Flat `[x,y,z,...]` positions as little-endian `f32` bytes, base64-encoded.
     */
    positionsB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.RawMeshExport_positions_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Flat normals as little-endian `f32` bytes, base64-encoded.
     */
    normalsB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.RawMeshExport_normals_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Flat UVs as little-endian `f32` bytes, base64-encoded.
     */
    uvsB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.RawMeshExport_uvs_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Flat vertex colors as little-endian `f32` bytes, base64-encoded.
     */
    colorsB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.RawMeshExport_colors_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Triangle indices as little-endian `u32` bytes, base64-encoded.
     */
    indicesB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.RawMeshExport_indices_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Raw RGBA texture pixels, base64-encoded.
     */
    textureRgbaB64() {
        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);

    wasm.RawMeshExport_texture_rgba_b64(this.ffiValue, write.buffer);

        try {
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            write.free();
        }
    }

    /**
     * Width of the baked texture in pixels.
     */
    textureWidth() {

        const result = wasm.RawMeshExport_texture_width(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Height of the baked texture in pixels.
     */
    textureHeight() {

        const result = wasm.RawMeshExport_texture_height(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}