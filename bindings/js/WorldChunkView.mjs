// generated by diplomat-tool
import { NucleationError } from "./NucleationError.mjs"
import { Schematic } from "./Schematic.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const WorldChunkView_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.WorldChunkView_destroy(ptr);
});

/**
 * A single decoded chunk (or a from-scratch chunk under construction).
 */
export class WorldChunkView {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("WorldChunkView is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            WorldChunkView_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Create an empty chunk view at the given chunk coordinates — the
     * starting point for generating worlds from scratch. Sections are
     * created on demand by `set_block`. Serialized with
     * `status = "minecraft:full"` (Minecraft will not regenerate over it)
     * and the default data version.
     */
    static create(cx, cz) {

        const result = wasm.WorldChunkView_create(cx, cz);

        try {
            return new WorldChunkView(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * The chunk X coordinate (in chunk units).
     */
    cx() {

        const result = wasm.WorldChunkView_cx(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * The chunk Z coordinate (in chunk units).
     */
    cz() {

        const result = wasm.WorldChunkView_cz(this.ffiValue);

        try {
            return result;
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Convert the chunk view to a standalone schematic.
     */
    toSchematic() {

        const result = wasm.WorldChunkView_to_schematic(this.ffiValue);

        try {
            return new Schematic(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Set a block at absolute world coordinates inside this chunk view.
     * `block_name` must be a valid Minecraft block identifier (e.g.
     * `minecraft:stone`). Errors with `InvalidArgument` if (x, z) is outside
     * this chunk's column.
     */
    setBlock(x, y, z, blockName) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const blockNameSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, blockName)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldChunkView_set_block(diplomatReceive.buffer, this.ffiValue, x, y, z, blockNameSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Overwrite the biome of every currently-present section of the chunk
     * view with `biome_name` (e.g. `minecraft:desert`). Sections are created
     * lazily by `set_block`, so call this AFTER placing blocks.
     */
    setBiome(biomeName) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const biomeNameSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, biomeName)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.WorldChunkView_set_biome(diplomatReceive.buffer, this.ffiValue, biomeNameSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Deduped union of all sections' biome palette entries, in order of
     * first appearance, written as a JSON array string (`[]` if no section
     * carries biome data).
     */
    biomePaletteJson() {
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);

        const write = new diplomatRuntime.DiplomatWriteBuf(wasm);


        const result = wasm.WorldChunkView_biome_palette_json(diplomatReceive.buffer, this.ffiValue, write.buffer);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return write.readString8();
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            diplomatReceive.free();
            write.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}