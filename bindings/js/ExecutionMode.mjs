// generated by diplomat-tool
import { NucleationError } from "./NucleationError.mjs"
import { OutputCondition } from "./OutputCondition.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const ExecutionMode_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.ExecutionMode_destroy(ptr);
});

/**
 * How long to run the circuit for one `execute` call (PORTING rule 10).
 */
export class ExecutionMode {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("ExecutionMode is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            ExecutionMode_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Run for exactly `ticks` ticks.
     */
    static fixedTicks(ticks) {

        const result = wasm.ExecutionMode_fixed_ticks(ticks);

        try {
            return new ExecutionMode(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Run until the output named `output_name` meets `condition`,
     * checking every `check_interval` ticks, giving up after `max_ticks`
     * ticks (the result's `condition_met` reports which happened).
     */
    static untilCondition(outputName, condition, maxTicks, checkInterval) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const outputNameSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, outputName)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.ExecutionMode_until_condition(diplomatReceive.buffer, outputNameSlice.ptr, condition.ffiValue, maxTicks, checkInterval);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
            return new ExecutionMode(diplomatRuntime.internalConstructor, diplomatRuntime.ptrRead(wasm, diplomatReceive.buffer), []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    /**
     * Run until any output changes from its initial reading, checking
     * every `check_interval` ticks, giving up after `max_ticks` ticks.
     */
    static untilChange(maxTicks, checkInterval) {

        const result = wasm.ExecutionMode_until_change(maxTicks, checkInterval);

        try {
            return new ExecutionMode(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Run (one tick at a time) until all outputs have been unchanged for
     * `stable_ticks` consecutive ticks, giving up after `max_ticks`
     * ticks (the result's `condition_met` reports stability).
     */
    static untilStable(stableTicks, maxTicks) {

        const result = wasm.ExecutionMode_until_stable(stableTicks, maxTicks);

        try {
            return new ExecutionMode(diplomatRuntime.internalConstructor, result, []);
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}