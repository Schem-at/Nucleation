// generated by diplomat-tool
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";



/**
 * The closed interval a field's values are analytically proven to lie in.
 */
export class FieldRange {
    #min;
    get min() {
        return this.#min;
    }
    #max;
    get max() {
        return this.#max;
    }
    #internalConstructor(structObj, internalConstructor) {
        if (typeof structObj !== "object") {
            throw new Error("FieldRange's constructor takes an object of FieldRange's fields.");
        }

        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("FieldRange is an out struct and can only be created internally.");
        }
        if ("min" in structObj) {
            this.#min = structObj.min;
        } else {
            throw new Error("Missing required field min.");
        }

        if ("max" in structObj) {
            this.#max = structObj.max;
        } else {
            throw new Error("Missing required field max.");
        }

        return this;
    }

    // Return this struct in FFI function friendly format.
    // Returns an array that can be expanded with spread syntax (...)
    _intoFFI(
        dst,
        functionCleanupArena,
        appendArrayMap
    ) {

        this._writeToArrayBuffer(wasm.memory.buffer, dst, functionCleanupArena, appendArrayMap);

        return dst;
    }

    static get _sizeBytes() {
        return 8;
    }

    /// Currently unused, we may want to use later on though:
    static get _sizeAlign() {
        return 4;
    }

    static _fromSuppliedValue(internalConstructor, obj) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("_fromSuppliedValue cannot be called externally.");
        }

        if (obj instanceof FieldRange) {
            return obj;
        }

        return FieldRange.fromFields(obj);
    }

    _writeToArrayBuffer(
        arrayBuffer,
        offset,
        functionCleanupArena,
        appendArrayMap
    ) {
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 0, this.#min, Float32Array);
        diplomatRuntime.writeToArrayBuffer(arrayBuffer, offset + 4, this.#max, Float32Array);
    }

    // This struct contains borrowed fields, so this takes in a list of
    // "edges" corresponding to where each lifetime's data may have been borrowed from
    // and passes it down to individual fields containing the borrow.
    // This method does not attempt to handle any dependencies between lifetimes, the caller
    // should handle this when constructing edge arrays.
    static _fromFFI(internalConstructor, ptr) {
        if (internalConstructor !== diplomatRuntime.internalConstructor) {
            throw new Error("FieldRange._fromFFI is not meant to be called externally. Please use the default constructor.");
        }
        let structObj = {};
        const minDeref = (new Float32Array(wasm.memory.buffer, ptr, 1))[0];
        structObj.min = minDeref;
        const maxDeref = (new Float32Array(wasm.memory.buffer, ptr + 4, 1))[0];
        structObj.max = maxDeref;

        return new FieldRange(structObj, internalConstructor);
    }


    constructor(structObj, internalConstructor) {
        return this.#internalConstructor(...arguments)
    }
}