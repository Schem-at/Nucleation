# Bus solver corpus

26 scenarios, **12 solved**, **14 unsolved**. Generated by `build_gallery.py` on 2026-08-10.

Every bus cell in every entry was produced by `nucleation.Design.route_bus` / `route_bus_adapted` and verified by driving real values through the baked artifact in mc-tick. Where the router could not do the job the entry is UNSOLVED and names the blocking assumption; no geometry was hand-authored and presented as solver output.

## Ledger

| entry | verdict | scenario | sim cases | cells | wire | delay rt | skew rt | footprint | solve |
|---|---|---|---|---|---|---|---|---|---|
| `C01_cfg_default` | **SOLVED** | Config A: defaults | 12/12 | 416 | 184 | 3 | 0 | 1440 | 33 ms |
| `C02_cfg_yband` | **UNSOLVED** | Config B: y_band layer assignment | 12/12 | 416 | 184 | 3 | 0 | 1440 | 33 ms |
| `C03_cfg_budget` | **UNSOLVED** | Config C: max_len_rt delay budget of 2 rt | 12/12 | 416 | 184 | 3 | 0 | 1440 | 33 ms |
| `C04_cfg_materials` | **SOLVED** | Config D: material policy (structural + transparent block) | 12/12 | 416 | 184 | 3 | 0 | 1440 | 34 ms |
| `O01_wall_gap` | **SOLVED** | Obstacle avoidance: a full-height pier on the direct path | 12/12 | 320 | 144 | 2 | 0 | 768 | 3 ms |
| `O02_serpentine` | **SOLVED** | Congestion: staggered walls force a serpentine | 12/12 | 416 | 184 | 3 | 0 | 1440 | 33 ms |
| `O03_flat_obstacle` | **UNSOLVED** | FLAT bundle + obstacle: routed, DRC-clean, does not conduct | 1/12 | 310 | 138 | 2 | 2 | 1680 | 18 ms |
| `P01_reverse8` | **UNSOLVED** | Bit reversal: an 8-lane butterfly from the port declarations | n/a | - | - | - | - | - | 0 ms |
| `P02_permute8` | **SOLVED** | Arbitrary 8-lane permutation (eight 1-bit buses, hardest first) | 12/12 | 830 | 368 | 5 | 0 | 19308 | 19 ms |
| `P02b_permute8_declorder` | **UNSOLVED** | Control: the same permutation in declaration order | n/a | 720 | 319 | 5 | 0 | 17664 | 18 ms |
| `P03_slice_hi_lo` | **SOLVED** | Bit-slicing: 8-bit word -> low nibble + high nibble | 24/24 | 192 | 88 | 1 | 1 | 216 | 1 ms |
| `U01_analog_route` | **UNSOLVED** | Routing an ANALOG value (signal strength) between two ports | n/a | - | - | - | - | - | 0 ms |
| `U02_cost_weights` | **UNSOLVED** | Choosing cost weights: length vs delay vs footprint | 12/12 | 416 | 184 | 3 | 0 | 1440 | 32 ms |
| `U03_exclusion_zone` | **UNSOLVED** | A declared exclusion zone (RoutingRegion) the router must respect | 12/12 | 416 | 184 | 3 | 0 | 1440 | 33 ms |
| `V01_v2h` | **SOLVED** | Vertical stack -> flat plane (the router stamps the pivot) | 12/12 | 592 | 254 | 11 | 8 | 7680 | 4 ms |
| `V02_h2v` | **SOLVED** | Flat plane -> vertical stack (the pivot, reversed) | 12/12 | 864 | 374 | 13 | 10 | 7728 | 6 ms |
| `V03_flat90` | **SOLVED** | Flat X -> flat Z: the in-plane 90-degree corner | 12/12 | 1488 | 644 | 13 | 10 | 13984 | 12 ms |
| `V03b_flat90_tight` | **UNSOLVED** | The same corner, 14 blocks tighter: refused | n/a | - | - | - | - | - | 5 ms |
| `V04_v2h_4bit` | **SOLVED** | Vertical -> flat at width 4 (probing the fixed pivot width) | 16/16 | 216 | 93 | 6 | 3 | 1920 | 3 ms |
| `X01_cross8` | **SOLVED** | Two 8-bit buses crossing at 90 degrees | 24/24 | 416 | 176 | 1 | 0 | 462 | 9 ms |
| `X02_hex_cross_vertical` | **UNSOLVED** | 8-bit binary bundle crossing a hex analog trunk (vertical form) | 5/17 | 480 | 216 | 3 | 0 | 2016 | 67 ms |
| `X02b_hex_idle_vertical` | **SOLVED** | Control: the same crossing with the hex trunk idle | 12/12 | 480 | 216 | 3 | 0 | 2016 | 63 ms |
| `X03_hex_cross_flat8` | **UNSOLVED** | Eight flat lanes crossing a hex analog trunk (8 crossings) | 0/17 | 314 | 140 | 2 | 2 | 2200 | 80 ms |
| `Z01_level_change_tight` | **UNSOLVED** | Climb 8 levels with 5 blocks of horizontal room | n/a | - | - | - | - | - | 0 ms |
| `Z02_torch_ladder_up` | **UNSOLVED** | Dense vertical UP: 8 bits, 8 levels, one xz cell per bit | 256/256 | - | - | - | - | - | 0 ms |
| `Z03_ring_riser_down` | **UNSOLVED** | Descending: a 180-degree tiled ring riser, 2 bits on one 3x3 | 4/4 | - | - | - | - | - | 0 ms |

`coherence` is absent from every row on purpose: `BusCostVector` is computed in `src/design.rs` but no binding exposes a reader, so the router's own number cannot be read. Length, footprint and cell counts are measured by the harness off the emitted blocks; delay and skew come from `bus_skew`.

## Crossings

Two signals that cannot both go straight.

### `X01_cross8` &mdash; SOLVED

**Two 8-bit buses crossing at 90 degrees**

Bus A runs +X at z=8, bus B runs +Z at x=8, both in the dense 2y vertical form.  They cannot both go straight. Does the router find the crossing without being told?

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:lime_concrete'}))
# bus_b -> routed
d.route_bus('bus_b', driver='b_in', sinks=['b_out'], style=n.Style(**{'bus_block': 'minecraft:cyan_concrete', 'transparent_block': 'minecraft:cyan_stained_glass'}))
```

Config: `bus_a.style = {"bus_block": "minecraft:lime_concrete"}`; `bus_b.style = {"bus_block": "minecraft:cyan_concrete", "transparent_block": "minecraft:cyan_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 176 |
| delay_rt | 1 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 462 |
| artifact blocks | 512 |
| solver wall clock | 8.9 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 24/24 cases |

- bus_a conducts: **12/12**
- bus_b conducts: **12/12**

The reference case: `showcase/bus_cross8_design.schem` in the README is the same design, so a regression here means the harness is wrong, not the router.

Artifact: `artifacts/X01_cross8.schem` &nbsp; Render: `renders/X01_cross8.png`

### `X02_hex_cross_vertical` &mdash; UNSOLVED

**8-bit binary bundle crossing a hex analog trunk (vertical form)**

The hex analog stage occupies y=0..1 at x=6..8.  Can the router take an 8-bit vertical bundle straight through that corridor with the analog value still exact at the tap?

```
# bus_bin -> routed
d.route_bus('bus_bin', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:lime_concrete', 'transparent_block': 'minecraft:lime_stained_glass'}))
```

Config: `bus_bin.style = {"bus_block": "minecraft:lime_concrete", "transparent_block": "minecraft:lime_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 216 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 2016 |
| artifact blocks | 664 |
| solver wall clock | 67.2 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 5/17 cases |

- binary lanes intact while hex carries 11: **3/12**
- hex value exact while the bundle carries 0xAA: **2/5**

**Blocked by:** routed, but verification failed: 5/17 cases

Fixture `hex0` (hex_comb_stage), NOT solver output: probe_hex_transmit.py::Rig (66/66) against corpus/TRANSMIT002_hex_transmit_flat.schem

The hex trunk is a FIXTURE (probe_hex_transmit.py::Rig, 66/66), not router output; only the binary bundle is solved.

MEASURED OUTCOME: the router routes it, `check()` is clean, and bit 0 comes out stuck HIGH whenever the trunk carries a value. The mechanism is exact and reproducible: the router detoured bit 0 to z=-1 and put its dust at (7,2,-1), (8,2,-1), (9,2,-1), which are DIAGONALLY adjacent to the trunk's live readout dust at (8,1,0) -- one down, one over, which is a conducting dust diagonal.  The router avoided the trunk's OCCUPIED cells correctly and then ran its own wire alongside a live one.

X02b is the control: with the trunk unpowered the identical route passes 12/12.  So the defect is clearance, not geometry, and `NetClassRule.spacing` -- the field that exists for exactly this -- never reaches the router (see U03).

Artifact: `artifacts/X02_hex_cross_vertical.schem` &nbsp; Render: `renders/X02_hex_cross_vertical.png`

### `X02b_hex_idle_vertical` &mdash; SOLVED

**Control: the same crossing with the hex trunk idle**

X02's route, with every hex injector off.  Does the bundle conduct when its neighbour is quiet?

```
# bus_bin -> routed
d.route_bus('bus_bin', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:lime_concrete', 'transparent_block': 'minecraft:lime_stained_glass'}))
```

Config: `bus_bin.style = {"bus_block": "minecraft:lime_concrete", "transparent_block": "minecraft:lime_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 216 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 2016 |
| artifact blocks | 664 |
| solver wall clock | 63.3 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- binary lanes intact, hex trunk unpowered: **12/12**

Fixture `hex0` (hex_comb_stage), NOT solver output: probe_hex_transmit.py::Rig (66/66) against corpus/TRANSMIT002_hex_transmit_flat.schem

Read together with X02: same cells, same DRC, and the only difference is whether the analog carrier is live.

Artifact: `artifacts/X02b_hex_idle_vertical.schem` &nbsp; Render: `renders/X02b_hex_idle_vertical.png`

### `X03_hex_cross_flat8` &mdash; UNSOLVED

**Eight flat lanes crossing a hex analog trunk (8 crossings)**

A flat 2-pitch 8-bit bundle runs +X at y=2; the hex trunk runs -Z through x=10..12.  Every lane has to get past the trunk independently.  Does the router solve all eight, and does the analog value still arrive exact?

> Prediction was **solved**, outcome was **unsolved**.

```
# bus_bin -> routed
d.route_bus('bus_bin', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:magenta_concrete', 'transparent_block': 'minecraft:magenta_stained_glass'}))
```

Config: `bus_bin.style = {"bus_block": "minecraft:magenta_concrete", "transparent_block": "minecraft:magenta_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 140 |
| delay_rt | 2 |
| skew_rt | 2 |
| coherence | not exposed by the bridge |
| footprint | 2200 |
| artifact blocks | 498 |
| solver wall clock | 79.5 ms |
| check | CheckReport(clean=False, drc=0, rules=0) |
| simulation | 0/17 cases |

- 8 flat lanes intact while hex carries 7: **0/12**
- hex value exact while the bundle carries 0x55: **0/5**

**Blocked by:** routed, but verification failed: 0/17 cases

Fixture `hex0` (hex_comb_stage), NOT solver output: probe_hex_transmit.py::Rig (66/66) against corpus/TRANSMIT002_hex_transmit_flat.schem

Same fixture as X02.  If this one fails where X02 passes, the limit is per-lane crossing in the flat form, not the crossing idea.

Artifact: `artifacts/X03_hex_cross_flat8.schem` &nbsp; Render: `renders/X03_hex_cross_flat8.png`

## Permutations

The bits arrive somewhere other than where they left.

### `P01_reverse8` &mdash; UNSOLVED

**Bit reversal: an 8-lane butterfly from the port declarations**

`a_in` stacks bit 0 at the bottom, `a_out` stacks bit 0 at the TOP (step -2y).  Can the router route a full reversal, with all eight lanes crossing, and keep them isolated?

> Prediction was **solved**, outcome was **unsolved**.

```
# bus_rev -> failed: unsupported bus form: this design realizes the verified vertical 2y-pitch stack (step (0,2,0)), and can adapt a HORIZONTAL row onto it; port `a_out` has step (0, -2, 0), which is neither
d.route_bus('bus_rev', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:orange_concrete', 'transparent_block': 'minecraft:orange_stained_glass'}))
```

Config: `bus_rev.style = {"bus_block": "minecraft:orange_concrete", "transparent_block": "minecraft:orange_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | - |
| delay_rt | - |
| skew_rt | - |
| coherence | not exposed by the bridge |
| footprint | - |
| artifact blocks | - |
| solver wall clock | 0.1 ms |
| check | not reached |
| simulation | not reached |

**Blocked by:** bus_rev -> failed: unsupported bus form: this design realizes the verified vertical 2y-pitch stack (step (0,2,0)), and can adapt a HORIZONTAL row onto it; port `a_out` has step (0, -2, 0), which is neither

`map` is identity because the check is per DECLARED bit index: the reversal is geometric.  A walking-ones sweep is what proves it -- a bus that shorted two lanes would light two lamps.

### `P02_permute8` &mdash; SOLVED

**Arbitrary 8-lane permutation (eight 1-bit buses, hardest first)**

Route a_i -> b_[3, 1, 4, 7, 0, 6, 2, 5].  Can eight independent 1-bit buses realize an arbitrary shuffle through one shared volume without shorting?

```
# net_a0 -> routed
d.route_bus('net_a0', driver='a0', sinks=['b3'])
# net_a1 -> routed
d.route_bus('net_a1', driver='a1', sinks=['b1'])
# net_a2 -> routed
d.route_bus('net_a2', driver='a2', sinks=['b4'])
# net_a3 -> routed
d.route_bus('net_a3', driver='a3', sinks=['b7'])
# net_a4 -> routed
d.route_bus('net_a4', driver='a4', sinks=['b0'])
# net_a5 -> routed
d.route_bus('net_a5', driver='a5', sinks=['b6'])
# net_a6 -> routed
d.route_bus('net_a6', driver='a6', sinks=['b2'])
# net_a7 -> routed
d.route_bus('net_a7', driver='a7', sinks=['b5'])
```

| metric | value |
|---|---|
| length (wire cells) | 368 |
| delay_rt | 5 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 19308 |
| artifact blocks | 878 |
| solver wall clock | 19.1 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- shuffle a_i -> b_P[i], all 8 lanes isolated: **12/12**

Walking ones is the isolation proof: if two nets shorted, one lever would light two lamps and the read word would not match the permutation.  Net order: [3, 4, 6, 0, 2, 7, 5, 1].

Artifact: `artifacts/P02_permute8.schem` &nbsp; Render: `renders/P02_permute8.png`

### `P02b_permute8_declorder` &mdash; UNSOLVED

**Control: the same permutation in declaration order**

P02's nets, reordered.  Does the solver recover from a bad net order on its own?

```
# net_a0 -> routed
d.route_bus('net_a0', driver='a0', sinks=['b3'])
# net_a1 -> routed
d.route_bus('net_a1', driver='a1', sinks=['b1'])
# net_a2 -> routed
d.route_bus('net_a2', driver='a2', sinks=['b4'])
# net_a3 -> routed
d.route_bus('net_a3', driver='a3', sinks=['b7'])
# net_a4 -> failed: segment (1, 10, 4) -> (46, 2, 4): the anchors are 8 level(s) apart and every placement of the 23-cell level-shift tile was blocked: level shift at the driver end ((2, 10, 4) -> (24, 2, 4)): collision at (9, 6, 4): `minecraft:redstone_wire[east=side,north=none,power=0,south=none,west=side]` (bus `net_a0`) already there, wanted `minecraft:redstone_wire[east=none,north=none,power=0,south=none,west=none]`; level shift mid-run ((12, 10, 4) -> (34, 2, 4)): collision at (13, 8, 4): `minecraft:redstone_wire[east=side,north=none,power=0,south=none,west=side]` (bus `net_a0`) already there, wanted `minecraft:gray_concrete`; level shift at the sink end ((23, 10, 4) -> (45, 2, 4)): collision at (24, 8, 4): `minecraft:repeater[facing=west,delay=1,locked=false,powered=false]` (bus `net_a0`) already there, wanted `minecraft:gray_concrete`
d.route_bus('net_a4', driver='a4', sinks=['b0'])
# net_a5 -> routed
d.route_bus('net_a5', driver='a5', sinks=['b6'])
# net_a6 -> routed
d.route_bus('net_a6', driver='a6', sinks=['b2'])
# net_a7 -> routed
d.route_bus('net_a7', driver='a7', sinks=['b5'])
```

| metric | value |
|---|---|
| length (wire cells) | 319 |
| delay_rt | 5 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 17664 |
| artifact blocks | - |
| solver wall clock | 18.0 ms |
| check | not reached |
| simulation | not reached |

**Blocked by:** net_a4 -> failed: segment (1, 10, 4) -> (46, 2, 4): the anchors are 8 level(s) apart and every placement of the 23-cell level-shift tile was blocked: level shift at the driver end ((2, 10, 4) -> (24, 2, 4)): collision at (9, 6, 4): `minecraft:redstone_wire[east=side,north=none,power=0,south=none,west=side]` (bus `net_a0`) already there, wanted `minecraft:redstone_wire[east=none,north=none,power=0,south=none,west=none]`; level shift mid-run ((12, 10, 4) -> (34, 2, 4)): collision at (13, 8, 4): `minecraft:redstone_wire[east=side,north=none,power=0,south=none,west=side]` (bus `net_a0`) already there, wanted `minecraft:gray_concrete`; level shift at the sink end ((23, 10, 4) -> (45, 2, 4)): collision at (24, 8, 4): `minecraft:repeater[facing=west,delay=1,locked=false,powered=false]` (bus `net_a0`) already there, wanted `minecraft:gray_concrete`

The blocking assumption: nets are routed greedily in the order given, with no rip-up-and-retry.  Ordering is the caller's problem, and nothing in the API says so.

### `P03_slice_hi_lo` &mdash; SOLVED

**Bit-slicing: 8-bit word -> low nibble + high nibble**

Split one 8-bit driver into two 4-bit sinks: `lo` takes bits 0-3 (lsb-aligned), `hi` takes bits 4-7 (shift -4). Both need `truncate`, since each drops half the word.

```
# bus_hi -> routed
d.raw.route_bus_adapted('bus_hi', 'a_in', 'hi_out', '[]', '{"bus_block": "minecraft:yellow_concrete"}', align=2, shift=-4, truncate=True)
# bus_lo -> routed
d.raw.route_bus_adapted('bus_lo', 'a_in', 'lo_out', '[]', '{"bus_block": "minecraft:light_blue_concrete"}', align=0, shift=0, truncate=True)
```

Config: `bus_lo.style = {"bus_block": "minecraft:light_blue_concrete"}`; `bus_lo.width = {"align": 0, "shift": 0, "truncate": true}`; `bus_hi.style = {"bus_block": "minecraft:yellow_concrete"}`; `bus_hi.width = {"align": 2, "shift": -4, "truncate": true}`

| metric | value |
|---|---|
| length (wire cells) | 88 |
| delay_rt | 1 |
| skew_rt | 1 |
| coherence | not exposed by the bridge |
| footprint | 216 |
| artifact blocks | 240 |
| solver wall clock | 0.6 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 24/24 cases |

- high nibble = word >> 4: **12/12**
- low nibble = word & 0xF: **12/12**

`truncate=True` is required: without it the router REFUSES a lossy connection, which is the right default -- losing a word's high bits is not the router's call.

Artifact: `artifacts/P03_slice_hi_lo.schem` &nbsp; Render: `renders/P03_slice_hi_lo.png`

## Form changes

Vertical stack, flat plane, and the corners between.

### `V01_v2h` &mdash; SOLVED

**Vertical stack -> flat plane (the router stamps the pivot)**

`a_in` is a dense 2y stack, `a_out` is a flat 2-pitch row along +X.  Does the router detect the form mismatch and insert a v2h pivot on its own?

```
# bus_pivot -> routed
d.route_bus('bus_pivot', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:pink_concrete', 'transparent_block': 'minecraft:pink_stained_glass'}))
```

Config: `bus_pivot.style = {"bus_block": "minecraft:pink_concrete", "transparent_block": "minecraft:pink_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 254 |
| delay_rt | 11 |
| skew_rt | 8 |
| coherence | not exposed by the bridge |
| footprint | 7680 |
| artifact blocks | 640 |
| solver wall clock | 3.5 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- stack -> plane, bit order preserved: **12/12**

`pivot_for()` in crates/nucleation-routing/src/pivot.rs picks V2H.

Artifact: `artifacts/V01_v2h.schem` &nbsp; Render: `renders/V01_v2h.png`

### `V02_h2v` &mdash; SOLVED

**Flat plane -> vertical stack (the pivot, reversed)**

A flat 2-pitch row along +X drives a dense 2y stack.  Is the reverse pivot available, or is only one direction implemented?

```
# bus_pivot -> routed
d.route_bus('bus_pivot', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:purple_concrete', 'transparent_block': 'minecraft:purple_stained_glass'}))
```

Config: `bus_pivot.style = {"bus_block": "minecraft:purple_concrete", "transparent_block": "minecraft:purple_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 374 |
| delay_rt | 13 |
| skew_rt | 10 |
| coherence | not exposed by the bridge |
| footprint | 7728 |
| artifact blocks | 912 |
| solver wall clock | 6.2 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- plane -> stack, bit order preserved: **12/12**

Artifact: `artifacts/V02_h2v.schem` &nbsp; Render: `renders/V02_h2v.png`

### `V03_flat90` &mdash; SOLVED

**Flat X -> flat Z: the in-plane 90-degree corner**

Both ends are flat 2-pitch, but one spreads its bits along X and the other along Z.  Does the router take the corner without swapping lane order?

```
# bus_corner -> routed
d.route_bus('bus_corner', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:red_concrete', 'transparent_block': 'minecraft:red_stained_glass'}))
```

Config: `bus_corner.style = {"bus_block": "minecraft:red_concrete", "transparent_block": "minecraft:red_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 644 |
| delay_rt | 13 |
| skew_rt | 10 |
| coherence | not exposed by the bridge |
| footprint | 13984 |
| artifact blocks | 1536 |
| solver wall clock | 11.5 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- corner turned, lane order preserved: **12/12**

The corner needs room: with the sink 24 blocks out the same call fails with an internal plan conflict inside the form adapter -- see V03b.

Artifact: `artifacts/V03_flat90.schem` &nbsp; Render: `renders/V03_flat90.png`

### `V03b_flat90_tight` &mdash; UNSOLVED

**The same corner, 14 blocks tighter: refused**

How much room does the flat 90-degree corner need, and does the router say so before it fails?

```
# bus_corner -> failed: form adapter for `a_out`: internal plan conflict at (6, 4, 7): `minecraft:stone` vs `minecraft:repeater[facing=north,delay=1,locked=false,powered=false]`
d.route_bus('bus_corner', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:red_concrete', 'transparent_block': 'minecraft:red_stained_glass'}))
```

Config: `bus_corner.style = {"bus_block": "minecraft:red_concrete", "transparent_block": "minecraft:red_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | - |
| delay_rt | - |
| skew_rt | - |
| coherence | not exposed by the bridge |
| footprint | - |
| artifact blocks | - |
| solver wall clock | 5.0 ms |
| check | not reached |
| simulation | not reached |

**Blocked by:** bus_corner -> failed: form adapter for `a_out`: internal plan conflict at (6, 4, 7): `minecraft:stone` vs `minecraft:repeater[facing=north,delay=1,locked=false,powered=false]`

The blocking assumption: the pivot tiles are stamped from a fixed plan with no clearance query, so tight geometry fails late, inside the adapter.

### `V04_v2h_4bit` &mdash; SOLVED

**Vertical -> flat at width 4 (probing the fixed pivot width)**

V01 at width 4.  Is the v2h pivot parametric in width, or hard-wired to PIVOT_BITS = 8?

```
# bus_pivot4 -> routed
d.route_bus('bus_pivot4', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:pink_concrete'}))
```

Config: `bus_pivot4.style = {"bus_block": "minecraft:pink_concrete"}`

| metric | value |
|---|---|
| length (wire cells) | 93 |
| delay_rt | 6 |
| skew_rt | 3 |
| coherence | not exposed by the bridge |
| footprint | 1920 |
| artifact blocks | 240 |
| solver wall clock | 2.6 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 16/16 cases |

- 4-bit stack -> plane, exhaustive: **16/16**

PREDICTION DISPROVED. `PIVOT_BITS = 8` is the pivot TILE's own width, not a limit on the bus: a 4-bit bundle pivots and passes all 16 patterns. Recorded because the constant reads like a limit and is not one.

Artifact: `artifacts/V04_v2h_4bit.schem` &nbsp; Render: `renders/V04_v2h_4bit.png`

## Obstacles

Something solid is in the way.

### `O01_wall_gap` &mdash; SOLVED

**Obstacle avoidance: a full-height pier on the direct path**

A 3-deep solid pier blocks x=8, z=7..9, y=0..20 -- the whole straight path including every bit's support.  Does the router detour the whole bundle and keep it coherent?

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:lime_concrete', 'transparent_block': 'minecraft:lime_stained_glass'}))
```

Config: `bus_a.style = {"bus_block": "minecraft:lime_concrete", "transparent_block": "minecraft:lime_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 144 |
| delay_rt | 2 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 768 |
| artifact blocks | 431 |
| solver wall clock | 3.0 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts past the pier: **12/12**

The pier is loose-layer blocks, not a declared keepout: this tests that the router treats OCCUPIED cells as occupied.

Artifact: `artifacts/O01_wall_gap.schem` &nbsp; Render: `renders/O01_wall_gap.png`

### `O02_serpentine` &mdash; SOLVED

**Congestion: staggered walls force a serpentine**

Two walls with disjoint gaps.  Does the router find the S-shaped corridor, and what does the detour cost in delay and skew?

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:lime_concrete', 'transparent_block': 'minecraft:lime_stained_glass'}))
```

Config: `bus_a.style = {"bus_block": "minecraft:lime_concrete", "transparent_block": "minecraft:lime_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 33.0 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts through the S: **12/12**

This geometry is reused by the C-series configurability triptych, so the three configs are compared on the same problem.

Artifact: `artifacts/O02_serpentine.schem` &nbsp; Render: `renders/O02_serpentine.png`

### `O03_flat_obstacle` &mdash; UNSOLVED

**FLAT bundle + obstacle: routed, DRC-clean, does not conduct**

A flat 2-pitch 8-bit bundle has to climb a 1-wide wall. Does the reported route actually carry the bits?

```
# bus_flat -> routed
d.route_bus('bus_flat', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:magenta_concrete', 'transparent_block': 'minecraft:magenta_stained_glass'}))
```

Config: `bus_flat.style = {"bus_block": "minecraft:magenta_concrete", "transparent_block": "minecraft:magenta_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 138 |
| delay_rt | 2 |
| skew_rt | 2 |
| coherence | not exposed by the bridge |
| footprint | 1680 |
| artifact blocks | 394 |
| solver wall clock | 17.8 ms |
| check | CheckReport(clean=False, drc=2, rules=0) |
| simulation | 1/12 cases |

- walking ones through the detour: **1/12**

**Blocked by:** routed, but verification failed: 1/12 cases

Compare with the same bundle on a CLEAR path, which passes 12/12 -- so the defect is in the detour, not the flat form. Vertical-form bundles detour correctly (O01, O02).

Artifact: `artifacts/O03_flat_obstacle.schem` &nbsp; Render: `renders/O03_flat_obstacle.png`

## Vertical transport

Changing level: what the solver can price, and two verified forms it cannot reach.

### `Z01_level_change_tight` &mdash; UNSOLVED

**Climb 8 levels with 5 blocks of horizontal room**

The ports are 5 apart in x and 8 apart in y.  What does the solver do when a level change has nowhere to spread out?

```
# bus_climb -> failed: segment (1, 2, 4) -> (6, 10, 4): the anchors are 8 level(s) apart, so the bus must change level: the verified level-shift tile needs 23 cells of straight run (plus 1 to clear the port) on one horizontal axis, but the pair only spans 5 in x and 0 in z between (1, 2, 4) and (6, 10, 4). Lengthen the run, or split it with a gate so one leg has room
d.route_bus('bus_climb', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:light_gray_concrete'}))
```

Config: `bus_climb.style = {"bus_block": "minecraft:light_gray_concrete"}`

| metric | value |
|---|---|
| length (wire cells) | - |
| delay_rt | - |
| skew_rt | - |
| coherence | not exposed by the bridge |
| footprint | - |
| artifact blocks | - |
| solver wall clock | 0.2 ms |
| check | not reached |
| simulation | not reached |

**Blocked by:** bus_climb -> failed: segment (1, 2, 4) -> (6, 10, 4): the anchors are 8 level(s) apart, so the bus must change level: the verified level-shift tile needs 23 cells of straight run (plus 1 to clear the port) on one horizontal axis, but the pair only spans 5 in x and 0 in z between (1, 2, 4) and (6, 10, 4). Lengthen the run, or split it with a gate so one leg has room

The blocking assumption: ONE level-change mechanism, priced at 2 horizontal cells per y.  The router's error is exemplary -- it names the tile, the run it needs, the run available, and two ways out -- but there is no denser form for it to fall back to.

### `Z02_torch_ladder_up` &mdash; UNSOLVED

**Dense vertical UP: 8 bits, 8 levels, one xz cell per bit**

The climb Z01 refused, in the form that is already measured. Does it carry all eight bits, and can the solver ask for it?

```
```

| metric | value |
|---|---|
| length (wire cells) | - |
| delay_rt | - |
| skew_rt | - |
| coherence | not exposed by the bridge |
| footprint | - |
| artifact blocks | 120 |
| solver wall clock | 0.0 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 256/256 cases |

- all 256 patterns climb 8 levels, no bit lost or shorted: **256/256**

**Blocked by:** The form is verified in simulation on this very build, but no `route_bus` call can produce it: the router has one level-change mechanism (the 2-cells-per-y level-shift tile) and no way to select the torch ladder.

Fixture `ladder` (torch_ladder_bus), NOT solver output: vforms.py, probed 33/33 in probe_vertical_forms.py (8 towers x all 256 patterns)

Density: 1.000 xz cell per bit per level, vs the level-shift tile's 2 horizontal cells per y for the whole bundle. The constraint that makes it work is at the PORTS, not the towers -- entries must alternate +z/-z sides or the pitch-1 dusts merge into a T that stops pointing into the torch bases and the whole array reads a constant.

Artifact: `artifacts/Z02_torch_ladder_up.schem` &nbsp; Render: `renders/Z02_torch_ladder_up.png`

### `Z03_ring_riser_down` &mdash; UNSOLVED

**Descending: a 180-degree tiled ring riser, 2 bits on one 3x3**

Descent has no active carrier.  Does the passive ring riser carry two bits down 8 levels with the 180-degree phase offset, and can the solver ask for it?

```
```

| metric | value |
|---|---|
| length (wire cells) | - |
| delay_rt | - |
| skew_rt | - |
| coherence | not exposed by the bridge |
| footprint | - |
| artifact blocks | 40 |
| solver wall clock | 0.0 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 4/4 cases |

- both bits descend 8 levels, neither dead nor shorted: **4/4**

**Blocked by:** Verified in simulation here, but unreachable from the solver: `route_bus` has no descending form other than the level-shift tile, and no way to express 'two bits sharing one ring at 180 degrees'.

Fixture `ring` (ring_riser_bus), NOT solver output: vforms.py, probed 26/26 in probe_spiral_tiling.py (legal() predictor matched all 17 sims)

Two bits on one 3x3 ring is 4.5 xz cells per bit; an 11x3 ring carries a byte at 4.125. Separation is the whole legality argument: 1 IS the step so the nets merge, 2 puts a foreign support directly above a dust and SEVERS both, 3 is the first legal value. A severed bit is simply quiet, so this entry checks arrival, not silence.

Artifact: `artifacts/Z03_ring_riser_down.schem` &nbsp; Render: `renders/Z03_ring_riser_down.png`

## Configurability

One problem, four configs, side by side.

### `C01_cfg_default` &mdash; SOLVED

**Config A: defaults**

The serpentine with nothing configured. Every other C entry changes exactly one thing against this baseline.

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'])
```

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 33.3 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts under this config: **12/12**

Compare `cells` / `delay_rt` / `footprint` against C02-C04.

Artifact: `artifacts/C01_cfg_default.schem` &nbsp; Render: `renders/C01_cfg_default.png`

### `C02_cfg_yband` &mdash; UNSOLVED

**Config B: y_band layer assignment**

Same problem, plus a net-class rule confining the bus to y 0..20. Does the route change, and does check() enforce the band?

> Prediction was **solved**, outcome was **unsolved**.

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'])
d.set_bus_rule('bus_a', **{'y_band': [0, 20]})
```

Config: `bus_a.rule = {"y_band": [0, 20]}`

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 32.8 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts under this config: **12/12**

Config effect vs `C01_cfg_default`: **ignored**

**Blocked by:** the bus routes and verifies, but the CONFIGURATION was ignored: geometry is identical to C01_cfg_default

If the geometry is byte-identical to C01, the rule was CHECKED after the fact rather than used to route -- which is what src/design.rs:4298 does.

Artifact: `artifacts/C02_cfg_yband.schem` &nbsp; Render: `renders/C02_cfg_yband.png`

### `C03_cfg_budget` &mdash; UNSOLVED

**Config C: max_len_rt delay budget of 2 rt**

Same problem with a deliberately tight 2 rt budget (the default route needs 3). Does the router route SHORTER to fit the budget, or route as before and report the violation?

> Prediction was **solved**, outcome was **unsolved**.

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'])
d.set_bus_rule('bus_a', **{'max_len_rt': 2})
```

Config: `bus_a.rule = {"max_len_rt": 2}`

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 33.4 ms |
| check | CheckReport(clean=False, drc=0, rules=1) |
| simulation | 12/12 cases |

- bundle conducts under this config: **12/12**

Config effect vs `C01_cfg_default`: **ignored**

**Blocked by:** the bus routes and verifies, but the CONFIGURATION was ignored: geometry is identical to C01_cfg_default

MEASURED ANSWER: it routes exactly as C01 -- byte-identical geometry, 3 rt -- and then REPORTS the violation: check() comes back clean=False with `bus `bus_a`: max bit delay 3rt exceeds max_len_rt 2rt`. So the budget is a post-hoc assertion, not a routing objective. That is a defensible design (fail loudly rather than silently), but it means a caller cannot ask for a faster route -- only be told the one they got is too slow.

Artifact: `artifacts/C03_cfg_budget.schem` &nbsp; Render: `renders/C03_cfg_budget.png`

### `C04_cfg_materials` &mdash; SOLVED

**Config D: material policy (structural + transparent block)**

Same problem, different materials. Does the router honour a per-bus material policy without changing the topology it found?

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'], style=n.Style(**{'bus_block': 'minecraft:blue_concrete', 'transparent_block': 'minecraft:blue_stained_glass'}))
```

Config: `bus_a.style = {"bus_block": "minecraft:blue_concrete", "transparent_block": "minecraft:blue_stained_glass"}`

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 34.1 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts under this config: **12/12**

Config effect vs `C01_cfg_default`: **ignored**

Cell counts identical to C01 with a different palette is the RIGHT answer here: materials are a policy, not a topology.

Artifact: `artifacts/C04_cfg_materials.schem` &nbsp; Render: `renders/C04_cfg_materials.png`

## Asked and refused

Capabilities the router does not have yet.

### `U01_analog_route` &mdash; UNSOLVED

**Routing an ANALOG value (signal strength) between two ports**

The hex tap carries 1..15 on one wire.  Can the router move that VALUE 14 blocks, the way it moves a bit?

```
# bus_analog -> not attempted
port declaration failed first
#   declare_in('hex_tap', anchor=(2, 1, 0), step=(0, 2, 0), width=1, ty='uint') refused: Exception: NucleationError.InvalidArgument
```

Config: `bus_analog.style = {"bus_block": "minecraft:brown_concrete"}`

| metric | value |
|---|---|
| length (wire cells) | - |
| delay_rt | - |
| skew_rt | - |
| coherence | not exposed by the bridge |
| footprint | - |
| artifact blocks | - |
| solver wall clock | 0.0 ms |
| check | not reached |
| simulation | not reached |

**Blocked by:** bus_analog -> not attempted: declare_in('hex_tap', anchor=(2, 1, 0), step=(0, 2, 0), width=1, ty='uint') refused: Exception: NucleationError.InvalidArgument

Fixture `hex0` (hex_comb_stage), NOT solver output: probe_hex_transmit.py::Rig (66/66) against corpus/TRANSMIT002_hex_transmit_flat.schem

TWO blocking assumptions, and the second one bites first.

1. PORTS MUST BE BACKED BY HARDWARE THE ROUTER RECOGNISES. `declare_input` over the tap is refused with InvalidArgument before any routing happens.  Measured: a dust cell with a LEVER beside it declares as an input; a dust cell on a LAMP declares as an output; a bare dust tap on stone is refused in both directions.  So the router cannot be connected to another mechanism's output at all -- only to lever/lamp banks it understands.  For a tool meant to route between real builds, this is the sharpest edge in the whole corpus.

2. NO ANALOG TYPE, NO ANALOG CARRIER.  Even given a port, every bus is bits-on-lanes: `IoType` has no signal-strength variant, and the router has no form that emits the value-preserving hex stage (mechanism 12 of notes-hex-transport.md), which is measured, verified and sitting in the corpus unused.

### `U02_cost_weights` &mdash; UNSOLVED

**Choosing cost weights: length vs delay vs footprint**

Ask for the latency-optimised weights (delay 16, skew 24, length 0.5). Does anything change against C01?

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'])
d.set_bus_rule('bus_a', **{'y_band': [0, 20], 'cost': {'length': 0.5, 'delay': 16.0, 'skew': 24.0, 'coherence': 3.0, 'footprint': 0.25}})
```

Config: `bus_a.rule = {"cost": {"coherence": 3.0, "delay": 16.0, "footprint": 0.25, "length": 0.5, "skew": 24.0}, "y_band": [0, 20]}`

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 32.3 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts under this config: **12/12**

Config effect vs `C01_cfg_default`: **ignored**

**Blocked by:** the bus routes and verifies, but the CONFIGURATION was ignored: geometry is identical to C01_cfg_default

If the cell count, delay and footprint equal C01 exactly, the weights were ignored -- the router is not tunable from any binding.

Artifact: `artifacts/U02_cost_weights.schem` &nbsp; Render: `renders/U02_cost_weights.png`

### `U03_exclusion_zone` &mdash; UNSOLVED

**A declared exclusion zone (RoutingRegion) the router must respect**

Name a routing region on the net class and ask the router to stay in it. Is the constraint honoured, checked, or dropped?

```
# bus_a -> routed
d.route_bus('bus_a', driver='a_in', sinks=['a_out'])
d.set_bus_rule('bus_a', **{'region': 'bus_north', 'spacing': 2, 'direction_bias': 'X'})
```

Config: `bus_a.rule = {"direction_bias": "X", "region": "bus_north", "spacing": 2}`

| metric | value |
|---|---|
| length (wire cells) | 184 |
| delay_rt | 3 |
| skew_rt | 0 |
| coherence | not exposed by the bridge |
| footprint | 1440 |
| artifact blocks | 968 |
| solver wall clock | 32.7 ms |
| check | CheckReport(clean=True, drc=0, rules=0) |
| simulation | 12/12 cases |

- bundle conducts under this config: **12/12**

Config effect vs `C01_cfg_default`: **ignored**

**Blocked by:** the bus routes and verifies, but the CONFIGURATION was ignored: geometry is identical to C01_cfg_default

Compare geometry with C01. Identical geometry plus a silent check means region/spacing/direction_bias are dead fields on the routing path.

Artifact: `artifacts/U03_exclusion_zone.schem` &nbsp; Render: `renders/U03_exclusion_zone.png`

## What the solver cannot do yet

The roadmap, worst first. Each item is the assumption a specific entry above ran into.

### 1. No clearance policy reaches the router

*Entries: X02 / U03*

The router avoids OCCUPIED cells correctly and then runs its own dust diagonally adjacent to a live foreign wire, which couples the two signals. `NetClassRule.spacing` exists for exactly this and is never read on the routing path. Until a clearance rule reaches the router, no bus can be trusted next to a neighbour that carries a signal.

### 2. A flat-form bundle that detours does not conduct, and still reports success

*Entries: O03 / X03*

One plain wall in front of a flat 2-pitch bundle is enough: the router returns state `routed`, `check()` is clean, and in simulation lanes are dead or shorted together. Vertical-form bundles detour correctly (O01, O02), so the defect is in the flat detour. A router that reports success for a bus that does not carry bits is the most damaging failure mode in this list.

### 3. One level-change mechanism, and it is the expensive one

*Entries: Z01 / Z02 / Z03*

The router changes level only with the level-shift tile, priced at 2 cells of straight horizontal run per y, so an 8-level climb between ports 5 apart is refused outright. Two denser forms are already built and measured in `vforms.py`: the torch ladder climbs at ONE xz cell per bit per level with no reach limit (256/256 here), and the ring riser descends passively at 1 y per cell where redstone has no active descending carrier at all (verified here too). Neither can be selected through any call. This is the largest capability gap in the corpus, and unlike the defects above it is purely additive work.

### 4. Every configuration knob except materials is decoration

*Entries: C02 / C03 / U02 / U03*

`NetClassRule` is read in `Design::check` only (src/design.rs ~4289): `y_band` and `max_len_rt` are reported after the fact, and `region`, `spacing` and `direction_bias` are read nowhere at all. `RoutingRegion` -- include/exclude zones with a legality predicate and Insign authoring -- has no consumer in `design.rs`. And the cost weights are hard-coded: `route_bus` sets `cost: BusCost::default()` and neither the compact nor the latency-optimised preset can be selected from any binding, because `src/bridge/design.rs` contains no reader or writer for cost at all. Four entries here set a knob, verify, and come out with geometry identical to the baseline.

### 5. Ports must be lever banks and lamp banks

*Entries: U01*

`declare_input` needs a lever beside each bit's dust and `declare_output` needs a lamp under it; a bare dust tap is refused in both directions. So the router cannot be attached to another mechanism's output -- which is most of what daily work is.

### 6. No analog carrier and no analog type

*Entries: U01*

Every bus is bits-on-lanes. `IoType` has no signal-strength variant, and the router has no form that emits the hex comb stage -- a value-preserving analog carrier that is already measured, verified 66/66, and sitting in the corpus unused.

### 7. Net order is the caller's problem

*Entries: P02 / P02b*

The same eight-net permutation routes 8/8 hardest-first and 7/8 in declaration order, because nets are placed greedily with no rip-up-and-retry. Nothing in the API mentions ordering.

### 8. Descending port steps are refused outright

*Entries: P01*

A sink declared msb-first (step `(0,-2,0)`) is refused: "this design realizes the verified vertical 2y-pitch stack ... port `a_out` has step (0, -2, 0), which is neither". A bundle-level reversal or shuffle has to be decomposed into 1-bit nets by hand (P02), which costs a long straight run per net.

### 9. Form adapters have no clearance query

*Entries: V03b*

The flat 90-degree corner routes with the sink 38 blocks out and fails with an internal plan conflict at 24. The error names a cell, not a clearance, so the only way to find the limit is to try.

### 10. The router's own cost vector is invisible

*Entries: every entry*

`BusCostVector` (length / delay_rt / skew_rt / coherence / footprint) is computed in src/design.rs and has a `to_json`, but no binding exposes a reader. Every number in this gallery except delay and skew is measured by the harness off the emitted cells, and `coherence` is left null rather than faked.

## Reproducing

```sh
# a wheel built WITH routing (the released one has no Design)
NUCLEATION_FEATURES=bridge-full,routing,hdl \
    <venv>/bin/pip install ./bindings/python

cd redstone-eda/corpus_gallery
python run_corpus.py          # ~4 s, one subprocess per scenario
python render.py              # PNG per artifact (needs pack.zip)
python build_gallery.py       # index.html + REPORT.md
```
