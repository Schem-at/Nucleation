// generated by diplomat-tool
import { Brush } from "./Brush.mjs"
import { NucleationError } from "./NucleationError.mjs"
import { Schematic } from "./Schematic.mjs"
import { Shape } from "./Shape.mjs"
import wasm from "./diplomat-wasm.mjs";
import * as diplomatRuntime from "./diplomat-runtime.mjs";

const BuildingTool_box_destroy_registry = new FinalizationRegistry((ptr) => {
    wasm.BuildingTool_destroy(ptr);
});

/**
 * Namespace for the fill operations that combine a schematic, a shape and a
 * brush (the old `buildingtool_*` free functions).
 */
export class BuildingTool {
    // Internal ptr reference:
    #ptr = null;

    // Lifetimes are only to keep dependencies alive.
    // Since JS won't garbage collect until there are no incoming edges.
    #selfEdge = [];

    #internalConstructor(symbol, ptr, selfEdge) {
        if (symbol !== diplomatRuntime.internalConstructor) {
            console.error("BuildingTool is an Opaque type. You cannot call its constructor.");
            return;
        }
        this.#ptr = ptr;
        this.#selfEdge = selfEdge;

        // Are we being borrowed? If not, we can register.
        if (this.#selfEdge.length === 0) {
            BuildingTool_box_destroy_registry.register(this, this.#ptr);
        }

        return this;
    }
    /** @internal */
    get ffiValue() {
        return this.#ptr;
    }


    /**
     * Fill `shape` into `schematic` using `brush`.
     */
    static fill(schematic, shape, brush) {
    wasm.BuildingTool_fill(schematic.ffiValue, shape.ffiValue, brush.ffiValue);

        try {}

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Fill `count` copies of `shape`, each offset by `offset * i`.
     */
    static rstack(schematic, shape, brush, count, offsetX, offsetY, offsetZ) {
    wasm.BuildingTool_rstack(schematic.ffiValue, shape.ffiValue, brush.ffiValue, count, offsetX, offsetY, offsetZ);

        try {}

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Masked fill that preserves everything already placed: `brush` is
     * only written where `schematic` currently has air (or nothing at
     * all), so existing structures inside `shape` survive untouched.
     */
    static fillOnlyAir(schematic, shape, brush) {
    wasm.BuildingTool_fill_only_air(schematic.ffiValue, shape.ffiValue, brush.ffiValue);

        try {}

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
        }
    }

    /**
     * Masked fill that only overwrites the listed blocks: `targets_json`
     * is a JSON array of block ids (e.g. `["minecraft:stone"]`, state
     * properties ignored) and every cell of `shape` whose current block
     * id is in the list is replaced by `brush` — everything else,
     * including air, is left alone.
     */
    static fillReplacing(schematic, shape, brush, targetsJson) {
        let functionCleanupArena = new diplomatRuntime.CleanupArena();

        const targetsJsonSlice = functionCleanupArena.alloc(diplomatRuntime.DiplomatBuf.sliceWrapper(wasm, diplomatRuntime.DiplomatBuf.str8(wasm, targetsJson)));
        const diplomatReceive = new diplomatRuntime.DiplomatReceiveBuf(wasm, 5, 4, true);


        const result = wasm.BuildingTool_fill_replacing(diplomatReceive.buffer, schematic.ffiValue, shape.ffiValue, brush.ffiValue, targetsJsonSlice.ptr);

        try {
            if (!diplomatReceive.resultFlag) {
                const cause = new NucleationError(diplomatRuntime.internalConstructor, diplomatRuntime.enumDiscriminant(wasm, diplomatReceive.buffer));
                throw new globalThis.Error('NucleationError.' + cause.value, { cause });
            }
        }

        finally {
            diplomatRuntime.FUNCTION_PARAM_ALLOC.clean();
            functionCleanupArena.free();

            diplomatReceive.free();
        }
    }

    constructor(symbol, ptr, selfEdge) {
        return this.#internalConstructor(...arguments)
    }
}